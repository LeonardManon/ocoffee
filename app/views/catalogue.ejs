<%- include('partials/header') %>
<main>
    <section class="catalog">
        <h1> Notre catalogue </h1>
    </section>
        <div class="new-product-catalog">
            <% catalogue.forEach((product_coffee) => { %>
            <article class="product-catalog">
                <h3 class="product-title"><%= product_coffee.name %></h3>
                <img class="img-catalog" src="/integration/images/coffees/<%= product_coffee.reference%>.png" alt="photo-reference">
                <p> <%= product_coffee.description %></p>
                <a href="/produit/<%= product_coffee.id %>">Découvrir </a>
            </article>
            <% }) %>
        </div>
</main>
<%- include('partials/footer') %>